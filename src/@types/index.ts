export interface IconProps {
    focused:boolean;
    width: number;
    height: number;
    text: string;
}
export interface SectionItem {
    title:string;
    description: string;
    image: string;
}
export interface PagerItem {
    title:string;
    description: string;
    image: string;
    id: number;
}
